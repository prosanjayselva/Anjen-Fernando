# Game2Grow Base Setup

## Apps
- `apps/web`: React + TypeScript + Tailwind + Zustand + TanStack Query
- `apps/api`: Node.js + Express + TypeScript API for lead capture

## Run
1. Install dependencies at root: `npm install`
2. Start API: `npm run dev:api`
3. Start Web: `npm run dev:web`
